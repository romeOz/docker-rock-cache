- name: Installing dependencies
  shell: composer require romeoz/rock-cache:* chdir={{ root_dir }}
  tags: demo

- name: chown project
  shell: chown {{ username }}:{{ username }} {{ root_dir }} -Rf
  tags: demo